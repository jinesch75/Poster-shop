// (Removed 2026-05-02 along with /admin/settings — the hero-poster picker
// it backed wasn't actually read by the homepage. Kept as an empty
// module because the sandbox can't unlink files under iCloud Drive;
// queued for `rm -f lib/settings.ts` on Jacques' terminal.)

export {};
